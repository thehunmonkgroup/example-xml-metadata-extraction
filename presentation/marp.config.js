/** Lightweight config for ClueCon slides */
module.exports = {
  // Look for local themes in ./themes
  themeSet: './themes',

  // Default theme when none is specified in a deck
  theme: 'cluecon-zen',

  // Allow <span style=""> etc. in Markdown if you want subtle tweaks
  html: true,

  // Let Marp embed local images / fonts
  allowLocalFiles: true,
};

